---
name: image scan

on:
  workflow_call:

jobs:
  image-scan:
    runs-on: ubuntu-latest
    steps:

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}


      - name: Scan for vulnerabilities
        uses: crazy-max/ghaction-container-scan@v4
        with:
          image: ${{vars.DOCKERHUB_USERNAME}}/${{github.event.repository.name}}:latest
          severity_threshold: CRITICAL

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{vars.DOCKERHUB_USERNAME}}/${{github.event.repository.name}}:latest
          exit-code: '1'
          severity: 'CRITICAL,HIGH'    

      - name: Scan image with Grype
        uses: anchore/scan-action@v7
        id: scan
        with:
          image: ${{vars.DOCKERHUB_USERNAME}}/${{github.event.repository.name}}:latest
          fail-build: true
          severity-cutoff: 'CRITICAL,HIGH'   